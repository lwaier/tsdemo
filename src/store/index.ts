import Vue from 'vue'
import Vuex from 'vuex'
import work from './work'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    
  },
  mutations: {
    
  },
  actions: {
    
  },
  modules: {
    work, //work页面需要的echarts数据
  }
})
